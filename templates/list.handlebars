<!DOCTYPE html>

<html>
<head>
  <title></title>
  <link rel="icon" href="data:;base64,iVBORw0KGgo=">
</head>
<body>
  <h1>{{folder}}</h1>
  <ul>
  {{#unless isTopDir}}
    <li><a href="/">/</a></li>
    <li><a href="../">..</a></li>
  {{/unless}}
  {{#each files}}
    <li>
      <a href="{{pathToFile}}">{{name}}{{#if isDir}}/{{/if}}</a>
    </li>
  {{/each}}
  </ul>
</body>
</html>
